#숫자 카드 게임
#NXM 행렬 입력받은 후, NXM 행렬 입력하면 각 행에서 가장 작은 수 중 가장 큰 숫자를 출력

import sys
n,m=map(int,sys.stdin.readline().split())
minimum=[]
for i in range(n):
  input=list(map(int,sys.stdin.readline().split()))
  minimum.append(min(input))
print(max(minimum))
